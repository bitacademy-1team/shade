package com.one.shade.repository;

import com.one.shade.domain.Seasons;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SeasonsRepository extends JpaRepository<Seasons,Long> {
}
